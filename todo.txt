IN PROGRESS:

NEXT:
- handle RosterItem name collisions
- renaming
- selection handling (not for cli)

TO ASK:
- IOUtils package
- initialization in constructor vs at declaration (runBefore and normal)
- REQUIRES for input strings / index parameters
- comments?